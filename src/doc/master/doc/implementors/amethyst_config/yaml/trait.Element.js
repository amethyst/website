(function() {var implementors = {};
implementors["amethyst"] = ["impl Element for <a class=\"struct\" href=\"amethyst/gfx_device/struct.DisplayConfig.html\" title=\"struct amethyst::gfx_device::DisplayConfig\">DisplayConfig</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
